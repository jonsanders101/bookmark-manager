<h1> Here be links. </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %><br>
      URL:   <%= link.url   %><br>
      Tags:   <%= link.tags.map(&:name) %> <br>
    </li>
  <% end %>
</ul>
